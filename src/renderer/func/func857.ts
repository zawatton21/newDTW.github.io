import { Gvar } from '../variable'
import * as Adap from '../adapter/index'
import * as Func from '../func/index'

async function func857(this: any) {
        Adap.dbgprt(857);
        await Func.func858();
        Gvar.y_axis_map_image = Gvar.var_3261;
        Gvar.var_3263 = Gvar.y_axis_map_image;
        Adap.objsel(0);
        await Func.func852();
        return;
}

export {func857}
